document.addEventListener("DOMContentLoaded", function(event) {
    console.log("HIIII")

    const words = ['word0', 'word1', 'word2']

    const board = document.getElementById("div1")
    words.forEach(function(word) {
      board.append
      // append a node 
    })
  })
